# 概念验证驱动探索

## 核心思想

先列假设，再写代码验证，最后用验证结果直接决策。不做大而全的纸面对比。

## 流程

1. **列出关键假设** — 写下对候选方案的核心猜想，每条假设必须具体到可验证。  
   *反例：* "Flutter 性能更好"  
   *正例：* "用同一数据模型定义，OpenAPI Generator 在 Dart 和 TS 两端均零错误生成"

2. **逐个验证** — 针对每条假设，快速设计并实现 Demo。一个 PoC 可以覆盖多条相关假设。  
   *经验：* AI 辅助下，58 故事点的实验约 30 分钟完成。AI 降低了 PoC 成本，但不改变决策逻辑。

3. **汇总决策** — 根据验证结果直接决策。每条假设打 **+1**（优势）、**0**（持平）、**-1**（劣势），汇总看整体倾向。  
   *经验：* 简单的三分制足够，复杂的加权评分没有带来更好的决策。

## 经验法则

- **准备好失败应对** — 每条假设如果验证不通过，提前想好 Plan B。避免卡在一条假设上。
- **运行时可并行** — 互不依赖的 PoC 同时进行，缩短实验周期。
- **"持平"也是有效结论** — 很多假设最终不分胜负，这本身就是有用的决策信息。
- **不要追求大而全** — 验证 5 条关键假设比 20 条泛泛假设更有价值。
- **关注团队上下文** — 很多情况下，团队已有积累比技术优劣更影响决策。
- **架构方案从 PoC 中生长** — 不要预设立场，让验证结果推导出架构。最终方案是每条假设结论的自然组合。
